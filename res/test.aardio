//cmd命令回显
import process.popen;
import console;
  
if( testProcess ){
    testProcess.process.terminate();
    testProcess.close();
}

testProcess = process.popen("ping baidu.com") // 创建进程

if(!testProcess){
    console.log('错误的命令行或参数');
    return;
}

for( all,out,err in testProcess.each() ){
    console.log(out)
}

console.askYesNo("按Y键继续,按N键取消")